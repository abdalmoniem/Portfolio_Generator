{
   // See https://go.microsoft.com/fwlink/?LinkId=733558
   // for the documentation about the tasks.json format
   "version": "2.0.0",
   "tasks": [
      {
         "label": "watch and serve",
         "type": "shell",
         "isBackground": true,
         "command": "& '${command:python.interpreterPath}' scripts/watch.py",
         "options": {
            "cwd": "${workspaceFolder}",
         },
         "presentation": {
            "reveal": "never"
         }
      }
   ]
}